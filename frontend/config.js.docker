// Frontend Configuration
// Copy this file to config.js and update values as needed

const CONFIG = {
    // When running with Docker, the nginx proxy routes /api/* to the backend
    // For local development without Docker, point directly to the backend URL
    API_BASE_URL: '/api',
};

function getBaseHeaders() {
    return {
        'Content-Type': 'application/json',
    };
}
